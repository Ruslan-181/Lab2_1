#include <stdio.h>
#include <math.h>

int main(void) {
    //Кодування тексту щоб Українські літери нормально виводились на консоль
    system("chcp 1251 > nul");

    double x = 0.0;
    double y = 0.0;

    printf("Введіть значення x: ");
    if (scanf("%lf", &x) != 1) {
        printf("Помилка введення даних.\n");
        return 1;
    }

    // Базовий каркас програми
    printf("Значення x = %.2f успішно зчитано.\n", x);
    printf("Обчислення функції буде реалізовано у наступній версії.\n");

    return 0;
}
